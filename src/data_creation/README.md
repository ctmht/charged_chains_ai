To create a new dataset, use 'sequence_sampler.py' and adjust parameters in the main code as needed.
Afterwards, you can run 'dataframe_creator.py' to create dataframes which will be used as the main
source of truth for Signac.